const bouton = document.querySelectorAll('.bouton-modale');
const modale = document.querySelectorAll('modale');

for (let i = 0; index < bouton.length; i++) {
    const element = bouton[i];
    document.addEventListener('click', openModale);
}

function openModale (e) {
    
}